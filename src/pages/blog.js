import React from 'react';
import styled from 'styled-components';

const BlogPageWrapper = styled.div`

`;

const Blog = () =>(
  <BlogPageWrapper>
    <h1>Blog page</h1>
  </BlogPageWrapper>
);

export default Blog;
